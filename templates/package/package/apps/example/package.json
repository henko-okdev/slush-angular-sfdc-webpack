{
  "name": "ExampleApplication",
  "version": "1.0.0",
  "description": "",
  "main": "bundle.js",
  "scripts": {
    "build": "webpack --progress --colors --watch --config",
    "build:prod": "webpack --progress --colors --watch --config -p",
    "build:deploy": "webpack --progress --colors --watch --config webpack.sfdc.config.js",
    "build:prod:deploy": "webpack --progress --colors --watch --config webpack.sfdc.config.js -p"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "angular": "^1.6.2"
  },
  "devDependencies": {
    "css-loader": "^0.9.1",
    "less": "^2.5.0",
    "less-loader": "^2.2.0",
    "raw-loader": "^0.5.1",
    "style-loader": "^0.9.0",
    "webpack": "^1.14.0",
    "webpack-sfdc-deploy-plugin": "^1.0.0"
  }
}